import {Styles} from "./Styles";
import {Animate} from "./Animate";

export type Options = {
    timeout?: number
    animateFunction?: Animate
    styles?: Styles
    gap?: number
    elementShiftTime?: number
}